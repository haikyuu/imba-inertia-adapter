{
  "name": "imba-inertia-adapter",
  "version": "2.0.0-alpha.209",
  "main": "./dist/index.js",
  "dependencies": {
    "@inertiajs/inertia": "^0.11.0"
  },
  "peerDependencies": {
    "imba": "^2.0.0-alpha.208"
  },
  "scripts": {
    "build": "imbac src -o dist && npx esbuild src/Form.ts --outfile=dist/Form.js",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.14.42"
  }
}
